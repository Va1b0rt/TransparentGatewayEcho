events {}
http {
 resolver 127.0.0.11 ipv6=off;
 server {
  listen 8081;
  set_real_ip_from 0.0.0.0/0;
  real_ip_header X-Test-Rewrite;
  set $forward_scheme http;
  set $server echo;
  set $port 8080;
  include /etc/nginx/echo.conf;
 }
}
